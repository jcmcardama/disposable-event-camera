// src/app/admin/page.tsx
// Placeholder — full admin dashboard is built in Milestone 9.
// This stub exists only so Next.js's typed routes don't error on an empty file.

export default function AdminPage() {
  return <div>Admin dashboard coming soon.</div>;
}