// Shared TypeScript types used across the app.
// We start this file now and add to it as each milestone introduces
// new data shapes (Device, Photo, EventSettings, etc.)
// Keeping types centralized means the frontend and API routes always agree
// on what shape the data is in.

export {};